<Page x:Class="DeviceCenter.PageWelcome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DeviceCenter"
      mc:Ignorable="d" 
      d:DesignHeight="628.5" d:DesignWidth="1006.5"
      Title="Welcome">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="115*"/>
            <ColumnDefinition Width="534*"/>
            <ColumnDefinition Width="353*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="99" />
            <RowDefinition Height="416" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <FlowDocumentPageViewer x:Name="documentWelcome" ScrollViewer.VerticalScrollBarVisibility="Disabled" Margin="10" Grid.Column="1" Grid.Row="1" Grid.RowSpan="2" Background="{x:Null}" MaxZoom="198">
            <FlowDocumentPageViewer.Template>
                <ControlTemplate TargetType="{x:Type FlowDocumentPageViewer}">
                    <Border Margin="{TemplateBinding Padding}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
						VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                        <AdornerDecorator ClipToBounds="True">
                            <DocumentPageView PageNumber="0" 
								DocumentViewerBase.IsMasterPage="True"
								KeyboardNavigation.TabIndex="1" 
								KeyboardNavigation.TabNavigation="Local"
								KeyboardNavigation.IsTabStop="False"/>
                        </AdornerDecorator>
                    </Border>
                </ControlTemplate>
            </FlowDocumentPageViewer.Template>
            <FlowDocument PagePadding="0" >
                <Paragraph FontFamily="Segoe UI Light" FontSize="32" >
                    <Run Text="Discover the internet of your things"/>
                </Paragraph>
                <Paragraph FontFamily="Segoe UI Light" FontSize="14">
                    <Run Text="Rapidly prototype and build your Windows IoT solutions on a variety of devices running Windows 10 IoT Core. Windows 10 gives you powerful tools that let you develop fast and deploy to your device."/>
                </Paragraph>
                <Paragraph FontFamily="Segoe UI Light" FontSize="14">
                    <Run Text="Already have a Windows IoT device on the network? "/>
                    <Run Text=" "/>
                    <Run Text=" "/>
                    <Run Text=" "/>
                    <Hyperlink FontFamily="Segoe UI Light" FontSize="14" Click="ViewDevices_Click">
                        <Run Text="Find My Device"/>
                    </Hyperlink>
                </Paragraph>
                <BlockUIContainer>
                    <Button FontSize="20" FontFamily="Segoe UI Light" Padding="30 5 30 5" HorizontalAlignment="Left" Content="Set up a new device" Click="SetupDeviceButton_Click" Foreground="White" Background="#FF3277B8"/>
                </BlockUIContainer>
            </FlowDocument>
        </FlowDocumentPageViewer>
        <Image x:Name="imageFrontLogo" Grid.Column="2" Grid.Row="1" Source="Assets/front logo.png" MinWidth="120" MaxWidth="200" Margin="0"/>
    </Grid>
</Page>
