<Page x:Class="DeviceCenter.PageWelcome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DeviceCenter"
      xmlns:props="clr-namespace:DeviceCenter.Strings"
      mc:Ignorable="d" 
      d:DesignHeight="628.5" d:DesignWidth="1006.5"
      Title="{x:Static props:Strings.AppNameDisplay}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="628.666666666667" />
        </Grid.RowDefinitions>
        <DockPanel>
            <Viewbox Stretch="Fill" OpacityMask="Red" VerticalAlignment="Top" d:LayoutOverrides="VerticalAlignment">
                <TextBlock FontFamily="Segoe UI Light" Text="asdfasdfasdfasdf" FontSize="32" Height="629" Width="1007" Background="#FFCB4141"/>
            </Viewbox>
        </DockPanel>
        <FlowDocumentPageViewer Visibility="Collapsed" x:Name="documentWelcome" ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="{x:Null}" MaxZoom="198" Margin="0,0,-0.333,-0.333" d:IsHidden="True">
            <FlowDocumentPageViewer.Template>
                <ControlTemplate TargetType="{x:Type FlowDocumentPageViewer}">
                    <Border Margin="{TemplateBinding Padding}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
						VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                        <AdornerDecorator ClipToBounds="True">
                            <DocumentPageView PageNumber="0" 
								DocumentViewerBase.IsMasterPage="True"
								KeyboardNavigation.TabIndex="1" 
								KeyboardNavigation.TabNavigation="Local"
								KeyboardNavigation.IsTabStop="False"/>
                        </AdornerDecorator>
                    </Border>
                </ControlTemplate>
            </FlowDocumentPageViewer.Template>
            <FlowDocument>
                <Paragraph FontFamily="Segoe UI Light" FontSize="32" >
                    <Run Text="{x:Static props:Strings.IntroTitle}"/>
                </Paragraph>
                <Paragraph FontFamily="Segoe UI Light" FontSize="14">
                    <Run Text="{x:Static props:Strings.IntroSubTitle}"/>
                </Paragraph>
                <Paragraph FontFamily="Segoe UI Light" FontSize="14">
                    <Run Text="{x:Static props:Strings.IntroMessage}"/>
                    <Hyperlink FontFamily="Segoe UI Light" FontSize="14" Click="ViewDevices_Click">
                        <Run Text="{x:Static props:Strings.FindMyDeviceButton}"/>
                    </Hyperlink>
                </Paragraph>
                <BlockUIContainer>
                    <Button FontSize="20" FontFamily="Segoe UI Light" Padding="30 5 30 5" HorizontalAlignment="Left" Content="{x:Static props:Strings.NewDeviceTitle}" Click="SetupDeviceButton_Click" Foreground="White" Background="#FF3277B8"/>
                </BlockUIContainer>
            </FlowDocument>
        </FlowDocumentPageViewer>
    </Grid>
</Page>
